var total = 0L;
var i = 0;
while (i < __ITER__) {
    var numerator = ((base + i) * (multiplier - (i % modulus))) + offset;
    total = total + ((numerator - (numerator % divisor)) / divisor) + (bias * (i % 3));
    i = i + 1;
}
total
