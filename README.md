# FDUtilities

基于Taboolib开发的一个动态类、模组加载插件。


---

### 构建

* [Gradle](https://gradle.org/) - 依赖关系管理

**Windows:**

```
gradlew.bat clean build
```

**macOS/Linux:**

```
./gradlew clean build
```

构建工件在 `./outs` 文件夹中